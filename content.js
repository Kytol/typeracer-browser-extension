document.getElementById("tstats").innerHTML = "Press F11 & use tab and Enter to move through webpage!";

//Click anywhere=website full screen, fix bg bug to uncomment:
/* 
addEventListener("click", function() {
    var el = document.documentElement,
      rfs = el.requestFullscreen
        || el.webkitRequestFullScreen
        || el.mozRequestFullScreen
        || el.msRequestFullscreen 
    ;

    rfs.call(el);
});
*/